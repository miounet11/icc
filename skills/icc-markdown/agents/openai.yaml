interface:
  display_name: "icc Markdown Viewer"
  short_description: "Open markdown files in a formatted panel with live reload alongside the terminal."
  default_prompt: "Use this skill to display markdown plans, docs, or notes in a icc panel: write to a .md file, run 'icc markdown open <path>', and the panel auto-updates when the file changes."
