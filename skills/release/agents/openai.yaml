interface:
  display_name: "icc Release"
  short_description: "Prepare and publish icc releases from version bump to tag."
  default_prompt: "Use this skill to run the icc release workflow: choose version, generate user-facing changelog updates, bump versions, open and monitor release PRs, merge, tag, and verify release artifacts."
